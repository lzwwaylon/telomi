[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "schedule-review"
version = "0.1.0"
license = "Apache-2.0"
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["src/schedule_review"]
