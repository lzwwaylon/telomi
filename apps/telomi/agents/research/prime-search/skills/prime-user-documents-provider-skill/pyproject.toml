[project]
name = "prime-user-documents-provider-skill"
version = "0.1.0"
license = "Apache-2.0"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/prime_user_documents_provider_skill"]
