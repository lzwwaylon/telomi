Continue the same {{ mode_label }} report task as the same root Agent. Read the existing child drafts and finish every required writer-output Section body. Remove every square-bracket numeric citation such as `[1]` or `[1, 2]`; preserve only grounded `<cite>...</cite>` Evidence citations. Remove every other Agent-authored writer-output file, including report.md, manifest.json, and .complete. Runtime owns the manifest and completion marker. Write only sections/<section-id>.md files.
