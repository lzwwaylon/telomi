## 续跑上下文

上一次执行被中断。以下 Section 的 draft.md 与 ledger.md 已经完成并保留在 work/sections/ 下：

{{ completed_section_list }}

不要为这些 Section 派生子 Agent，也不要重写或删除它们的草稿。只为其余 Section 派生子 Agent。
如果 work/report-outline.json 已存在，它是上一次由你确定的结构；保持其 Section IDs 和顺序不变。
