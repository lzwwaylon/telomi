The final source audit file protocol did not pass. Repair only the concrete file or field reported by Runtime, then start one fresh final source auditor that overwrites work/source-audit-final.json. Use exact child model {{ child_model }}. Do not regenerate the plan or segment drafts.

{{ validation_error }}
