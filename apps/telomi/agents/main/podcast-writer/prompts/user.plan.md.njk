Load the podcast-writing Skill and execute Phase 1 only. Use exact child model {{ child_model }}. Create the episode plan and every assignment, then spawn exactly one RLM child per planned segment. Every child task must name its segment ID and instruct the child to read inputs/segment-contract.md before drafting; the contract defines the only accepted draft and ledger format. Stop this Root turn after all children have been started. Do not draft segment prose as Root.
