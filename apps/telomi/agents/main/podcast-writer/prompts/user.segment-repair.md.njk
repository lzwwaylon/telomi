Continue the same podcast task. Runtime rejected the file protocol at the exact location below. Inspect work/episode-plan.json and native rlm.list_subagents(). Use exact child model {{ child_model }}. Retry only the named missing or invalid segment child with a task that names its segment ID and instructs it to read inputs/segment-contract.md before drafting. Do not rewrite completed segment outputs. Stop after replacements are started.

{{ validation_error }}
