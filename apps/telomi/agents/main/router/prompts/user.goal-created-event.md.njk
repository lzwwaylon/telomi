[EVENT:GOAL_CREATED]
Maintain /work/topic-plan.json through the topic-plan Skill and native file Tools.
Goal context: {{ goal_context_json }}
Create a compact initial Topic document with multiple durable Topics, normally 3 to 6.
Split distinct long-term concerns into separate attention lenses instead of collapsing a broad Goal into one catch-all Topic.
Do not mirror Wiki Pages, workflow stages, or every subquestion as Topics. Use the smallest multiple set that preserves meaningful navigation.
Never start Research or scheduling before user confirmation.
For this lifecycle event turn only, after saving the file reply exactly [SILENT].
