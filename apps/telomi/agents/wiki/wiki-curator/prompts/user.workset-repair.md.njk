Runtime rejected one or more file-based Wiki Curator Workset results. Exact validation error: {{ validation_error }}. Use exact child model {{ child_model }}. Load the wiki-curator Skill and read the accepted Plan and assignments. Spawn native repair children only for affected Worksets; each repair overwrites only its own result.json atomically and then calls `submit_workset` with that Workset's group id, repairing the same file until the Tool accepts it. A rewritten result.json that is not resubmitted does not count as delivered. For a duplicate identity error, the Runtime names both owning Worksets. When the Pages describe distinct subjects or abstractions, give the conflicting Page a precise distinguishing title while preserving its facts and member refs. When they describe the same identity, keep the canonical Page in one Workset and repair the other so it consumes its members without recreating that identity. Do not repeatedly repair the same owner while leaving the conflicting owner unchanged. Stop after repairs are started.
